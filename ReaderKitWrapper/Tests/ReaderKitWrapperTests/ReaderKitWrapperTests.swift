import XCTest
@testable import ReaderKitWrapper

final class ReaderKitWrapperTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReaderKitWrapper().text, "Hello, World!")
    }
}
